ALTER table restaurants add column name varchar (256);
ALTER table restaurants add column genre varchar (256);
ALTER table restaurants add column level varchar (256);
ALTER table restaurants add column address varchar (256);
ALTER table restaurants add column telephone_number varchar (256);